name = "bephighcafe"
main = "src/index.js"
compatibility_date = "2026-07-21"

# `/api/*` is handled by the Worker; everything else comes from `public/`.
[assets]
directory = "./public"
binding = "ASSETS"
run_worker_first = ["/api/*"]
